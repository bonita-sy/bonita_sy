import sys

dx = [-1, 1, 0, 0]
dy = [0, 0, 1, -1]

def find_key():
	

def find_document(x, y, key_list, real_map, visited_map):
	if visited_map[x][y]:
		return document
	
	if 
		
	

if __name__=="__main__":
	tc_num = int(sys.stdin.readline().strip())
	for _ in range(tc_num):
		h, w = map(int, sys.stdin.readline().strip().split())
		real_map = [list(sys.stdin.readline().strip()) for _ in range(h)]
		visited_map = [[False] * w for _ in range(h)]

		key_list = list(sys.stdin.readline().strip())

		finded_document = 0
		

		find_document(0, 0, key_list, real_map, visited_map)
